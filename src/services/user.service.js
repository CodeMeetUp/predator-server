// TODO: Create a new function that wil accept data from the User DAO

// const creatUser = async (userObj) => {
//   try {
//     // TODO: Create a user object from the model
//     // User object should accept
//     // 1. firebaseUid, email, firtName, lastName,
//     // 2. Verify if the fireBaseUid or email already exist (should be unique)- return 404 if it exist
//     // 3. Pass the user object to the DAO function "create"
//     // 4. Return the user object if successful
//   } catch (error) {
//     // throw an error from this service
//   }
// };
